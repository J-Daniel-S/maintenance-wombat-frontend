import React, { useEffect } from "react";

const Tasks = (props) => {
    return(
        <main className="collection">
            <a href="#!" className="collection-item"><span className="new badge">1</span>Alan</a>
        </main>
    );
};

export default Tasks;