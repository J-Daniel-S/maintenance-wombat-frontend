import React, { useEffect } from 'react';

const Requester = (props) => {

    return (
        <React.Fragment >
            <p><bold>Requester page</bold></p>
        </React.Fragment>
    );
}

export default Requester;